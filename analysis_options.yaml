# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - 'build/**'
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
    # The Flutter SDK is vendored at the repository root as a gitlink, so
    # `flutter analyze` from the root walks into it and reports hundreds of
    # errors from the SDK's own test fixtures, which makes the command useless
    # as a signal. None of it is this project's Dart.
    - flutter/**
  errors:
    deprecated_member_use: ignore
    avoid_print: ignore
    unnecessary_underscores: ignore
    dangling_library_doc_comments: ignore
    use_null_aware_elements: ignore

linter:
  rules:
    prefer_single_quotes: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    sort_constructors_first: false
    require_trailing_commas: false

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
